use practice3;

-- create table orders(
--    id int primary key auto_increment,
--    customer_id int,
--    order_date date,
--    total_amount int
-- );

-- insert into orders(id , customer_id , order_date , total_amount)
-- values(1, 1, '2018-01-01', 100),
-- (2, 1, '2018-01-02', 200),
-- (3, 2, '2018-01-03', 300),
-- (4, 2, '2018-01-04', 400),
-- (5, 3, '2018-01-05', 500),
-- (6, 3, '2018-01-06', 600);

-- select customer_id , sum(total_amount) from orders group by customer_id order by sum(total_amount) desc


